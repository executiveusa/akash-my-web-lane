import { type ReactNode } from "react";
type HeaderProps = {
    pages: string[];
    page: string;
    children?: ReactNode;
};
export declare const Header: ({ pages, page, children }: HeaderProps) => import("react").JSX.Element;
export {};
//# sourceMappingURL=header.d.ts.map